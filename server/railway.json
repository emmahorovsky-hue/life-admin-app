{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "server/nixpacks.toml",
    "buildCommand": "npm run prisma:generate --workspace=server && npm run build --workspace=server",
    "watchPatterns": [
      "server/**",
      "packages/shared/**",
      "package.json",
      "package-lock.json"
    ]
  },
  "deploy": {
    "startCommand": "npm run prisma:migrate:deploy --workspace=server && npm run start --workspace=server",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
