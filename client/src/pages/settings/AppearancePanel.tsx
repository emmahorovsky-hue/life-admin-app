// Placeholder — theme switcher + default currency land in LIF-186.
export default function AppearancePanel() {
  return (
    <section className="rounded-[2px] border border-border bg-white p-6 dark:bg-card">
      <p className="font-mono text-[11px] uppercase tracking-[0.12em] text-muted-foreground">
        Coming soon
      </p>
      <p className="mt-2 text-sm text-muted-foreground">
        Theme and currency preferences are on their way.
      </p>
    </section>
  );
}
