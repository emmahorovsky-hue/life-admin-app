// App-wide constants

// Brand name lives in @life-admin/shared so web and mobile stay in step; this
// re-export keeps existing `@/lib/constants` imports across the client working.
export { APP_NAME } from '@life-admin/shared';
